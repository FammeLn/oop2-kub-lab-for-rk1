# 📘 Лекция №5: Service Discovery в Akka Cluster и Kubernetes

## Конспект лекции

---

## 1. Введение: Проблема обнаружения сервисов

### ❓ Почему Service Discovery неизбежен?

До этого момента мы изучали:
- **Akka Cluster** как логическое объединение узлов
- **Kubernetes** как среду, где Pod'ы могут появляться и исчезать
- Отсутствие «постоянных серверов»

**Фундаментальный вопрос:**
> Как один Pod узнаёт, где находятся другие Pod'ы, если у них нет постоянных IP-адресов?

Это и есть задача **Service Discovery**.

---

## 2. Почему традиционный подход не работает

### ❌ Проблемы фиксированных конфигураций

В традиционных системах:
- IP-адреса серверов **фиксированы**
- Адреса **прописываются в конфигурации**
- Инфраструктура **меняется редко**

### ⚠️ В Kubernetes это невозможно:

1. **Pod'ы создаются и уничтожаются динамически**
2. **IP-адрес Pod'а меняется при каждом перезапуске**
3. **Количество Pod'ов постоянно меняется**

> 💡 **Вывод:** Любая статическая конфигурация обречена на поломку

---

## 3. Что такое Service Discovery

### 📖 Определение

**Service Discovery** — это механизм, который позволяет сервису:
- 🔍 Найти другие экземпляры сервиса
- 📡 Получить их актуальные адреса
- 🔄 Реагировать на изменения состава системы

### 🎯 Service Discovery отвечает на вопросы:

1. **«Кто сейчас жив?»**
2. **«Куда можно подключиться?»**
3. **«Кто ушёл?»**

---

## 4. Service Discovery в Kubernetes

### 🛠️ Механизмы реализации

Kubernetes предоставляет Service Discovery через:
- **DNS** — разрешение имён сервисов
- **Объекты Service** — логические группы Pod'ов
- **API Kubernetes** — программный доступ к информации о Pod'ах

### 💡 Основная идея:

```
Приложение → Логическое имя сервиса → Kubernetes → Актуальные Pod'ы
```

**Приложение НЕ знает IP Pod'ов!**  
**Kubernetes сам управляет соответствием имени и Pod'ов.**

---

## 5. Почему Kubernetes Service недостаточно для Akka Cluster

### 🔄 Kubernetes Service предоставляет:
- ✅ Балансировку трафика (Load Balancing)
- ✅ Единое DNS-имя для группы Pod'ов
- ✅ Health checking

### ❌ Но Akka Cluster требуется другое:

1. **Узнать конкретные адреса узлов** (не один VIP)
2. **Установить прямые соединения между ними** (peer-to-peer)
3. **Поддерживать membership** (кто в кластере, кто вышел)

> 💡 **Load Balancer ≠ Cluster Discovery**

---

## 6. Задача: Bootstrap кластера

### 🚀 Что такое Cluster Bootstrap?

Для запуска кластера Akka необходимо:
1. Чтобы узлы **нашли друг друга**
2. Чтобы они **договорились о membership**
3. Чтобы кластер **сформировался автоматически**

Этот процесс называется **Cluster Bootstrap**.

### ⚙️ Без Bootstrap:

В старых версиях Akka использовались **seed-nodes**:
```hocon
akka.cluster.seed-nodes = [
  "akka://system@192.168.1.10:25520",
  "akka://system@192.168.1.11:25520"
]
```

❌ **Проблемы seed-nodes в Kubernetes:**
- IP-адреса меняются при рестарте
- Нужно обновлять конфигурацию при каждом изменении
- Не работает с динамическим масштабированием

---

## 7. Akka Management — мост между Akka и Kubernetes

### 📦 Что такое Akka Management?

**Akka Management** — специальный модуль, который:
- 🌐 Запускает **HTTP endpoint** внутри Pod'а
- 🔌 Взаимодействует с **Kubernetes API**
- 🤝 Помогает узлам **находить друг друга**

> 💡 Akka Management — это **служебный слой**, а не бизнес-логика

### 🔌 Management Endpoint:

```
http://<pod-ip>:8558/

Endpoints:
- /alive    - liveness probe
- /ready    - readiness probe
- /cluster  - информация о кластере
```

---

## 8. Зачем Akka Management вынесен отдельно?

### 🎯 Причины разделения:

1. **Не нужен вне Kubernetes**
   - Локальная разработка использует другие механизмы

2. **Опциональная зависимость**
   - Приложение остаётся независимым от инфраструктуры

3. **Гибкость архитектуры**
   - Можно менять discovery-механизм (Kubernetes, Consul, AWS, etc.)

4. **Разделение ответственности**
   - Akka Cluster — логика кластера
   - Akka Management — интеграция с платформой

---

## 9. Как работает Akka Cluster Bootstrap

### 🔄 Процесс формирования кластера:

```
1. Pod запускается
   ↓
2. Akka Management стартует HTTP endpoint (порт 8558)
   ↓
3. Узел запрашивает Kubernetes API
   ↓
4. Получает список Pod'ов (по label selector)
   ↓
5. Пытается подключиться к Management endpoints других Pod'ов
   ↓
6. Согласовывает membership
   ↓
7. Формирует кластер
```

> ✅ **Всё происходит автоматически!**

### 🔍 Детали процесса:

**Contact Point Discovery:**
- Kubernetes API возвращает список Pod'ов
- Фильтруются по label selector: `app=akka-cluster`
- Извлекаются IP-адреса и порты

**Contact Points:**
- Узлы связываются друг с другом через Management HTTP
- Проверяют готовность к формированию кластера
- Выбирают "лидера" для инициации join

**Join процесс:**
- Первый узел создаёт кластер (self-join)
- Остальные узлы присоединяются к нему
- Membership синхронизируется через Gossip

---

## 10. Децентрализация Bootstrap

### 🎯 Ключевой принцип:

**Нет «главного» Pod'а!**

- ✅ Любой Pod может стать первым
- ✅ Кластер формируется независимо от порядка запуска
- ✅ Нет single point of failure

### 💪 Преимущества:

1. **Отказоустойчивость** — нет критичного узла
2. **Масштабируемость** — можно добавлять узлы в любой момент
3. **Cloud-native** — соответствует принципам Kubernetes

---

## 11. Связь с предыдущей лекцией

| Лекция 4 (Кластер) | Лекция 5 (Discovery) |
|-------------------|---------------------|
| Что такое кластер | Как он формируется |
| Узлы | Поиск узлов |
| Membership | Bootstrap |
| Pod = узел | Pod = участник discovery |

---

## 12. Service Discovery как постоянный процесс

### ⚠️ Важно понимать:

Service Discovery:
- ✅ **Работает постоянно** (не разовая операция)
- ✅ **Реагирует на изменения** (новые Pod'ы, удалённые Pod'ы)
- ✅ **Обновляет информацию** о составе системы

### 🔄 Динамическая природа:

```
Pod добавлен → Discovery обнаруживает → Join к кластеру
Pod удалён → Gossip замечает → Unreachable → Removed
```

---

## 13. Типичные ошибки новичков

### ❌ Чего НЕ делать:

1. **Жёстко прописывать IP-адреса**
   ```hocon
   # ПЛОХО!
   akka.cluster.seed-nodes = ["akka://system@192.168.1.10:25520"]
   ```

2. **Использовать Kubernetes Service как seed-node**
   - Service — это load balancer, не список узлов

3. **Считать, что кластер «создан навсегда»**
   - Кластер динамический, состав постоянно меняется

4. **Игнорировать динамическую природу Pod'ов**
   - Pod'ы появляются и исчезают, это норма

---

## 14. Архитектура с Akka Management

### 📐 Структура Pod'а:

```
┌──────────────────────────┐
│ Pod                      │
│                          │
│ ┌──────────────────────┐ │
│ │ Akka Actor System    │ │
│ │ (Бизнес-логика)      │ │
│ └──────────────────────┘ │
│           ↕              │
│ ┌──────────────────────┐ │
│ │ Akka Cluster         │ │
│ │ (Membership)         │ │
│ └──────────────────────┘ │
│           ↕              │
│ ┌──────────────────────┐ │
│ │ Akka Management      │ │
│ │ (HTTP Endpoint)      │ │
│ └──────────┬───────────┘ │
└────────────┼─────────────┘
             │
             ↓
     Kubernetes API
     (Service Discovery)
```

---

## 15. Конфигурация: Ключевые моменты

### 🔧 application.conf:

```hocon
akka {
  # Remoting для связи между узлами
  remote.artery.canonical {
    hostname = ${HOSTNAME}  # IP Pod'а
    port = 25520
  }
  
  # Management HTTP endpoint
  management.http {
    hostname = "0.0.0.0"
    port = 8558
  }
  
  # Bootstrap конфигурация
  management.cluster.bootstrap {
    contact-point-discovery {
      discovery-method = kubernetes-api
    }
  }
  
  # Discovery через Kubernetes API
  discovery.kubernetes-api {
    pod-label-selector = "app=akka-cluster"
  }
}
```

---

## 16. RBAC: Права доступа

### 🔐 Почему нужен RBAC?

Для обращения к Kubernetes API Pod'у нужны права:
- **ServiceAccount** — идентификатор Pod'а
- **Role** — разрешения на операции
- **RoleBinding** — связь ServiceAccount с Role

### 📜 Необходимые права:

```yaml
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
```

❌ **Без этих прав Bootstrap не сработает!**

---

## 17. Итоги лекции

### 🎓 После лекции студент должен:

1. ✅ Понимать, **почему discovery обязателен** в Kubernetes
2. ✅ Знать **роль Akka Management**
3. ✅ Понимать процесс **bootstrap кластера**
4. ✅ Видеть отличие **load balancing** от **cluster discovery**
5. ✅ Быть готовым к **практической настройке**

---

## 18. Ключевые термины

| Термин | Определение |
|--------|------------|
| **Service Discovery** | Механизм обнаружения сервисов в распределённой системе |
| **Cluster Bootstrap** | Процесс автоматического формирования кластера |
| **Akka Management** | HTTP endpoint для управления и discovery |
| **Contact Points** | Узлы, через которые происходит инициация кластера |
| **Seed Nodes** | Старый способ (статический список узлов) |
| **RBAC** | Role-Based Access Control (управление доступом) |

---

## 19. Дополнительные материалы

### 📚 Рекомендуемое чтение:

- [Akka Management Documentation](https://doc.akka.io/docs/akka-management/current/)
- [Kubernetes Service Discovery](https://kubernetes.io/docs/concepts/services-networking/service/)
- [Akka Cluster Bootstrap](https://doc.akka.io/docs/akka-management/current/bootstrap/)

### 🔗 Связанные темы:

- **Лекция 4:** Akka Cluster basics
- **Лекция 6:** (следующая) Cluster Sharding
- **Лекция 7:** Distributed Data

---

## 20. Контрольные вопросы

### ❓ Проверьте своё понимание:

1. **Почему seed-nodes плохо подходят для Kubernetes?**
   <details>
   <summary>Ответ</summary>
   Потому что IP-адреса Pod'ов динамические и меняются при каждом перезапуске. Статическая конфигурация seed-nodes не работает с динамической природой Kubernetes.
   </details>

2. **Что делает Akka Management?**
   <details>
   <summary>Ответ</summary>
   Akka Management запускает HTTP endpoint для health checks, readiness probes и взаимодействия с Kubernetes API для Service Discovery. Это мост между Akka Cluster и Kubernetes.
   </details>

3. **Чем discovery отличается от балансировки?**
   <details>
   <summary>Ответ</summary>
   Load balancing распределяет запросы между серверами (один запрос → один сервер). Discovery предоставляет список всех узлов для установления прямых peer-to-peer соединений в кластере.
   </details>

4. **Почему bootstrap должен быть децентрализован?**
   <details>
   <summary>Ответ</summary>
   Децентрализация обеспечивает отказоустойчивость (нет single point of failure), гибкость (узлы могут запускаться в любом порядке) и соответствие cloud-native принципам.
   </details>

5. **Что произойдёт при удалении Pod'а?**
   <details>
   <summary>Ответ</summary>
   Gossip протокол обнаружит недоступность узла, пометит его как Unreachable, затем как Removed. Остальные узлы продолжат работу. При наличии Deployment, Kubernetes автоматически создаст новый Pod.
   </details>

---

**Создано:** 27 февраля 2026  
**Курс:** OOP2 — Объектно-ориентированное программирование 2  
**Тема:** Akka Cluster Bootstrap и Service Discovery в Kubernetes
