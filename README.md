# OOP2 - Лабораторные работы

Репозиторий с лабораторными работами по курсу "Объектно-ориентированное программирование 2".

## 📚 Содержание

### 🧪 Лабораторная работа №5: Actor Model
**Папка:** `oop2-lab5/`

**Тема:** Первый актор на Akka (Java)

**Цель:** Изучить основы акторной модели и научиться работать с фреймворком Akka.

**Ключевые концепции:**
- Акторная модель и её преимущества
- Message passing вместо вызова методов
- Изоляция состояния
- Отсутствие race condition без блокировок
- Akka Typed API

**Что реализовано:**
- ✅ Простой актор HelloActor
- ✅ Актор со счётчиком CounterActor
- ✅ Примеры отправки сообщений
- ✅ Эксперименты с 1000+ сообщений
- ✅ Демонстрация отсутствия race condition

**Файлы:**
- `README.md` — полное описание работы
- `QUICKSTART.md` — быстрый старт
- `LECTURE_NOTES.md` — конспект лекции
- Исходный код на Java с подробными комментариями

**Запуск:**
```bash
cd oop2-lab5
./run.sh          # Linux/Mac
# или
run.bat           # Windows
# или
mvn exec:java     # через Maven
```

📖 **[Перейти к лабораторной работе →](oop2-lab5/)**

---

### 🧪 Лабораторная работа №6: Взаимодействие акторов
**Папка:** `oop2-lab6/`

**Тема:** Как из акторов складывается система

**Цель:** Научиться строить системы из множества взаимодействующих акторов.

**Ключевые концепции:**
- ActorSystem как контейнер и среда выполнения
- ActorRef как единственный способ общения
- Создание дочерних акторов через context.spawn()
- Передача ActorRef между акторами
- Request-Response паттерн без блокировок
- Mailbox и последовательная обработка

**Что реализовано:**
- ✅ ServiceActor — обработка запросов
- ✅ ClientActor — получение ответов
- ✅ Request с полем replyTo (ActorRef)
- ✅ Асинхронное взаимодействие
- ✅ Эксперименты с задержками
- ✅ Демонстрация работы mailbox

**Файлы:**
- `README.md` — полное описание работы
- `QUICKSTART.md` — быстрый старт
- `LECTURE_NOTES.md` — конспект лекции
- Исходный код на Java с подробными комментариями

**Запуск:**
```bash
cd oop2-lab6
./run.sh          # Linux/Mac
# или
run.bat           # Windows
# или
mvn exec:java     # через Maven
```

📖 **[Перейти к лабораторной работе →](oop2-lab6/)**

---

### 🧪 Лабораторная работа №7: Управление состоянием актора
**Папка:** `oop2-lab7/`

**Тема:** Как хранить состояние без shared state

**Цель:** Научиться корректно управлять состоянием в акторах через behavior switching.

**Ключевые концепции:**
- Состояние как параметр поведения (не в полях!)
- Behavior switching — смена поведения
- Immutable state — неизменяемое состояние
- Актор как конечный автомат (FSM)
- Функциональный стиль внутри акторов
- Подготовка к Akka Persistence

**Что реализовано:**
- ✅ CounterState — immutable класс состояния
- ✅ CounterActor — с behavior switching
- ✅ Команды: Increment, Add, Reset, PrintValue
- ✅ Демонстрация FSM (Finite State Machine)
- ✅ 7 экспериментов с изменением состояния
- ✅ Демонстрация правильного подхода vs. неправильного

**Файлы:**
- `README.md` — полное описание работы
- `QUICKSTART.md` — быстрый старт
- `LECTURE_NOTES.md` — конспект лекции
- Исходный код на Java с подробными комментариями

**Запуск:**
```bash
cd oop2-lab7
./run.sh          # Linux/Mac
# или
run.bat           # Windows
# или
mvn exec:java     # через Maven
```

📖 **[Перейти к лабораторной работе →](oop2-lab7/)**

---

### 🚀 Лабораторная работа №5 (Kubernetes): Akka Cluster Bootstrap
**Папка:** `lab-kub5/`

**Тема:** Service Discovery и автоматическое формирование кластера в Kubernetes

**Цель:** Научиться создавать самоформирующиеся Akka кластеры в Kubernetes без статических seed-nodes.

**Ключевые концепции:**
- Service Discovery через Kubernetes API
- Cluster Bootstrap — автоматическое формирование кластера
- Akka Management — HTTP endpoint для управления
- RBAC конфигурация для доступа к K8s API
- Децентрализованный bootstrap без единой точки отказа
- Cloud-native архитектура
- Health checks и Readiness probes
- Динамическое масштабирование кластера

**Что реализовано:**
- ✅ ClusterNode — узел кластера с мониторингом событий
- ✅ Akka Management интеграция
- ✅ Cluster Bootstrap конфигурация
- ✅ Kubernetes манифесты (Namespace, RBAC, Service, Deployment)
- ✅ Dockerfile для контейнеризации
- ✅ Автоматизированные скрипты (build, deploy, cleanup)
- ✅ Эксперименты с масштабированием и resilience
- ✅ Management endpoints (/alive, /ready, /cluster)

**Технологии:**
- Akka 2.8.5 (Actor Typed, Cluster Typed)
- Akka Management 1.4.1 (Management, Bootstrap, Discovery)
- Kubernetes (Deployment, Service, RBAC)
- Docker (multi-stage build)
- Maven (build, packaging)

**Файлы:**
- `README.md` — полное описание работы
- `QUICKSTART.md` — быстрый старт
- `LECTURE_NOTES.md` — конспект лекции №5
- `Dockerfile` — образ приложения
- `k8s/*.yaml` — Kubernetes манифесты
- `scripts/*.sh` — автоматизация

**Запуск:**
```bash
cd lab-kub5

# 1. Сборка Docker образа
./scripts/build.sh

# 2. Деплой в Kubernetes
./scripts/deploy.sh

# 3. Проверка
kubectl get pods -n akka-cluster
kubectl logs -f -l app=akka-cluster -n akka-cluster

# 4. Очистка
./scripts/cleanup.sh
```

**Эксперименты:**
```bash
# Масштабирование
kubectl scale deployment/akka-cluster-deployment --replicas=5 -n akka-cluster

# Тест resilience
kubectl delete pod <pod-name> -n akka-cluster
```

📖 **[Перейти к лабораторной работе →](lab-kub5/)**

---

### 🚀 Лабораторная работа №6 (Kubernetes): Cluster Sharding
**Папка:** `lab-kub6/`

**Тема:** Распределённое состояние через Akka Cluster Sharding в Kubernetes

**Цель:** Научиться реализовывать stateful-логику через entity/shard и отправлять команды через sharding proxy.

**Ключевые концепции:**
- Entity как бизнес-сущность (`AccountActor`)
- Shard как техническая единица распределения
- Автоматическое создание entity по `entityId`
- Passivation неактивных entity
- Прозрачная маршрутизация сообщений через `EntityRef`
- Миграция и восстановление entity при изменении состава кластера

**Что реализовано:**
- ✅ `AccountActor` с состоянием баланса
- ✅ Команды `UpdateBalance` и `GetBalance`
- ✅ Инициализация `ClusterSharding` и `EntityTypeKey`
- ✅ Отправка сообщений в entity через `EntityRef`
- ✅ Kubernetes манифесты (Namespace, RBAC, Service, Deployment)
- ✅ Скрипты build/deploy/cleanup
- ✅ Документация (README, QUICKSTART, LECTURE_NOTES)

**Запуск:**
```bash
cd lab-kub6

# 1. Сборка образа
./scripts/build.sh

# 2. Деплой в Kubernetes
./scripts/deploy.sh

# 3. Проверка
kubectl get pods -n akka-sharding
kubectl logs -f -l app=akka-sharding -n akka-sharding

# 4. Очистка
./scripts/cleanup.sh
```

📖 **[Перейти к лабораторной работе →](lab-kub6/)**

---

## 🔧 Требования

### Для лабораторных работ 5, 6, 7:
- **Java:** 17 или выше
- **Maven:** 3.6 или выше

### Дополнительно для kubernetes-лабораторных (`lab-kub5`, `lab-kub6`):
- **Docker:** для сборки образов
- **Kubernetes:** один из вариантов:
  - Minikube (рекомендуется)
  - kind (Kubernetes in Docker)
  - Docker Desktop с Kubernetes
  - Удалённый кластер
- **kubectl:** для управления кластером

## 📝 Документация

Каждая лабораторная работа содержит:
- Подробный README с теорией
- Примеры кода с комментариями
- Инструкции по запуску
- Контрольные вопросы и ответы
- Дополнительные задания

---

**© 2026 OOP2 Course**