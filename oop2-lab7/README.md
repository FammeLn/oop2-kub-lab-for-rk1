# 🧪 Лабораторная работа №7
## Состояние и смена поведения акторов

### 📚 Цель работы
Научиться:
- Хранить состояние корректно (не в полях!)
- Реализовывать state machine
- Применять behavior switching
- Отказаться от mutable-полей
- Использовать immutable state

---

## 🎯 Ключевые концепции

### Проблема: Где хранить состояние?

**❌ НЕПРАВИЛЬНЫЙ подход (как делать НЕ надо):**
```java
class CounterActor {
    private int value = 0; // ❌ Mutable state в поле!
    
    Behavior<Command> behavior() {
        return Behaviors.receiveMessage(msg -> {
            value++; // ❌ Изменение поля
            return Behaviors.same();
        });
    }
}
```

**Почему это плохо:**
- Состояние скрыто в поле
- Логика размыта
- Сложнее тестировать
- Противоречит функциональному стилю

**Это "актор как объект", а не actor model!**

### ✅ ПРАВИЛЬНЫЙ подход

**Состояние = параметр поведения:**
```java
class CounterActor {
    static Behavior<Command> create() {
        return behavior(new CounterState(0));
    }
    
    private static Behavior<Command> behavior(CounterState state) {
        return Behaviors.receiveMessage(msg -> {
            CounterState newState = state.increment();
            return behavior(newState); // Behavior switching!
        });
    }
}
```

**Почему это правильно:**
- ✅ Состояние явно видно
- ✅ Нет скрытых изменений
- ✅ Легко тестировать
- ✅ Функциональный стиль
- ✅ Подготовка к Persistence

### Immutable State — обязательное правило

Состояние актора должно быть:
- **Immutable** (неизменяемо)
- **Заменяемым**, а не изменяемым
- **Простым объектом данных**

```java
class CounterState {
    final int value; // final = immutable
    
    CounterState(int value) {
        this.value = value;
    }
    
    // Каждое "изменение" возвращает НОВЫЙ объект
    CounterState increment() {
        return new CounterState(value + 1);
    }
}
```

### Behavior Switching — ключевая идея

**Behavior switching** означает:
- Актор может сменить своё поведение
- Следующее сообщение будет обработано по-другому
- Это аналог state machine, но безопасный и функциональный

```java
// При изменении состояния возвращаем НОВОЕ поведение
CounterState newState = state.increment();
return behavior(newState); // 🔁 Смена поведения!
```

### Актор как конечный автомат (FSM)

Теперь можно сказать:
> **Актор — это конечный автомат, реагирующий на сообщения**

- **Состояние** → Поведение
- **Сообщение** → Переход
- **Никаких блокировок**

---

## 🏗️ Структура проекта

```
oop2-lab7/
├── pom.xml                          # Maven конфигурация
├── README.md                        # Этот файл
└── src/
    └── main/
        ├── java/com/akka/lab7/
        │   ├── Command.java         # Интерфейс команд
        │   ├── Increment.java       # Команда +1
        │   ├── PrintValue.java      # Вывод значения
        │   ├── Reset.java           # Сброс в 0
        │   ├── Add.java             # Добавить значение
        │   ├── CounterState.java    # Immutable состояние
        │   ├── CounterActor.java    # Актор со state machine
        │   └── Main.java            # Эксперименты
        └── resources/
            ├── application.conf     # Конфигурация Akka
            └── logback.xml          # Логирование
```

---

## 💻 Запуск проекта

### Требования
- Java 17+
- Maven 3.6+

### Сборка и запуск
```bash
cd oop2-lab7

# Сборка
mvn clean compile

# Запуск
mvn exec:java
```

---

## 📖 Разбор кода

### 1. Immutable состояние

```java
class CounterState {
    final int value; // final → неизменяемо
    
    CounterState increment() {
        return new CounterState(value + 1); // Новый объект!
    }
    
    CounterState add(int amount) {
        return new CounterState(value + amount); // Новый объект!
    }
    
    CounterState reset() {
        return new CounterState(0); // Новый объект!
    }
}
```

**Ключевое:** Методы НЕ изменяют текущий объект, а возвращают НОВЫЙ.

### 2. CounterActor с behavior switching

```java
class CounterActor {
    // Создание с начальным состоянием
    static Behavior<Command> create() {
        return behavior(new CounterState(0));
    }
    
    // Поведение с текущим состоянием
    private static Behavior<Command> behavior(CounterState state) {
        return Behaviors.receiveMessage(msg -> {
            
            if (msg instanceof Increment) {
                // Создаём НОВОЕ состояние
                CounterState newState = state.increment();
                
                // Возвращаем НОВОЕ поведение
                return behavior(newState); // BEHAVIOR SWITCHING!
            }
            
            if (msg instanceof PrintValue) {
                System.out.println("Value = " + state.value);
                return Behaviors.same(); // Состояние не изменилось
            }
            
            // ...другие команды
        });
    }
}
```

**Ключевые моменты:**
- ❌ НЕТ полей для состояния
- ✅ Состояние передаётся как параметр
- ✅ `return behavior(newState)` = смена поведения
- ✅ Нет скрытых мутаций

### 3. Обработка всех команд

```java
// Increment: увеличить на 1
if (msg instanceof Increment) {
    return behavior(state.increment());
}

// Add: добавить amount
if (msg instanceof Add) {
    return behavior(state.add(((Add) msg).amount));
}

// Reset: сбросить в 0
if (msg instanceof Reset) {
    return behavior(state.reset());
}

// PrintValue: вывести без изменения
if (msg instanceof PrintValue) {
    System.out.println("Value = " + state.value);
    return Behaviors.same();
}
```

---

## 🔬 Эксперименты

Программа выполняет 7 экспериментов:

### Эксперимент 1: Инкрементирование
```java
counter.tell(new Increment()); // 0 → 1
counter.tell(new Increment()); // 1 → 2
counter.tell(new Increment()); // 2 → 3
```

### Эксперимент 2: Проверка значения
```java
counter.tell(new PrintValue()); // Вывод: 3
```

### Эксперимент 3: Добавление значения
```java
counter.tell(new Add(10));      // 3 → 13
counter.tell(new PrintValue()); // Вывод: 13
```

### Эксперимент 4: Комбинация операций
```java
counter.tell(new Increment());  // 13 → 14
counter.tell(new Increment());  // 14 → 15
counter.tell(new Add(5));       // 15 → 20
counter.tell(new PrintValue()); // Вывод: 20
```

### Эксперимент 5: Сброс
```java
counter.tell(new Reset());      // 20 → 0
```

### Эксперимент 6: Проверка после сброса
```java
counter.tell(new PrintValue()); // Вывод: 0
```

### Эксперимент 7: Работа после сброса
```java
counter.tell(new Increment());  // 0 → 1
counter.tell(new Add(7));       // 1 → 8
counter.tell(new PrintValue()); // Вывод: 8
```

---

## ❓ Контрольные вопросы

### 1. Почему нельзя хранить state в полях актора?
**Ответ:** Потому что:
- Это противоречит функциональному стилю акторной модели
- Состояние становится скрытым и неявным
- Сложнее тестировать и отлаживать
- Не готовит к Akka Persistence
- Размывает логику изменения состояния

Правильный подход: состояние = параметр поведения.

### 2. Что такое behavior switching?
**Ответ:** Behavior switching — это смена поведения актора при изменении состояния. Вместо мутации поля мы возвращаем новое поведение с новым состоянием:
```java
CounterState newState = state.increment();
return behavior(newState); // Смена поведения!
```
Следующее сообщение будет обработано уже с новым состоянием.

### 3. Чем актор похож на state machine?
**Ответ:** Актор — это конечный автомат (FSM):
- **Состояние** определяет текущее поведение
- **Сообщение** вызывает переход между состояниями
- **Behavior switching** = переход в новое состояние
- Нет блокировок, всё через сообщения
- Детерминированная обработка (по одному сообщению)

### 4. Почему state должен быть immutable?
**Ответ:** Immutable state гарантирует:
- Нет скрытых мутаций
- Безопасность при асинхронной обработке
- Легче отслеживать изменения
- Подготовка к Event Sourcing
- Возможность сохранения и восстановления состояния
- Функциональный стиль

### 5. Как behavior связан с состоянием?
**Ответ:** Behavior и состояние неразрывно связаны:
- Состояние передаётся как параметр поведения
- `Behavior<Command> behavior(CounterState state)`
- Каждое новое состояние → новое поведение
- `return behavior(newState)`
- Это и есть behavior switching

---

## 🚀 Почему это готовит к Persistence

Если состояние:
- ✅ Immutable
- ✅ Выражено явно
- ✅ Обновляется через события

То его:
- ✅ Легко сохранять
- ✅ Легко восстанавливать
- ✅ Легко воспроизводить

**Это прямая подготовка к Akka Persistence!**

---

## ❌ Частые ошибки студентов

1. **Хранить state в полях** — нарушает принцип
2. **Использовать static переменные** — shared state!
3. **Мутировать объекты** — нарушает immutability
4. **Пытаться "вернуть значение"** — используйте replyTo
5. **Забывать `return behavior(newState)`** — состояние не обновится

---

## 🔧 Дополнительные задания

1. **Добавьте команду `Decrement`** — уменьшение на 1
2. **Добавьте команду `Multiply(int factor)`** — умножение
3. **Добавьте команду `GetValue(ActorRef<Response> replyTo)`** — ответ сообщением
4. **Добавьте ограничения** — например, value не может быть отрицательным
5. **Создайте `HistoryActor`** — хранит историю изменений (List<Integer>)

---

## ✅ Итоги работы

После выполнения лабораторной работы вы:
- ✅ Понимаете, где хранится состояние актора
- ✅ Умеете реализовывать state machine
- ✅ Используете behavior switching
- ✅ Мыслите функционально внутри акторов
- ✅ Готовы к supervision и отказам
- ✅ Готовы к Akka Persistence

---

## 📚 Связь с предыдущими неделями

**Неделя 5:** Один актор, базовая модель  
**Неделя 6:** Взаимодействие акторов, Request-Response  
**Неделя 7:** State machine, behavior switching, immutable state

**Следующий шаг:** Supervision, обработка отказов, устойчивость

---

**© 2026 OOP2 Лабораторная работа №7**
