# 📘 ЛЕКЦИЯ 5 - Actor Model

## Ключевые концепции из лекции

### 1. Проблема, которую решает акторная модель
- Потоки и блокировки не масштабируются
- Immutability помогает, но не решает всё
- Асинхронность усложняет код
- Shared mutable state — корень проблем

### 2. Три фундаментальных правила акторов
Любой актор может:
1. Получать сообщения
2. Изменять своё внутреннее состояние
3. Отправлять сообщения другим акторам
4. Создавать новые акторы

**И ничего больше!**

### 3. Почему в акторной модели нет race condition
- Состояние принадлежит одному актору
- Сообщения обрабатываются последовательно
- Никто не может «залезть» в состояние напрямую
- Даже если сообщений много — они в очереди

### 4. Акторы vs Объекты

| Объект (ООП) | Актор |
|--------------|-------|
| Методы | Сообщения |
| Общая память | Изолированное состояние |
| Синхронизация | Очередь сообщений |
| Вызов | Асинхронная отправка |

### 5. Message Passing вместо вызова методов

**Раньше:**
```java
account.deposit(100);
```

**Теперь:**
```java
accountActor.tell(new Deposit(100));
```

Мы не вызываем метод — мы отправляем сообщение!

### 6. Почему message passing масштабируется
- Сообщения дешёвые
- Нет блокировок
- Акторы легко распределяются
- Система устойчива к нагрузке

### 7. Typed Actors
Мы используем Typed API (не untyped) потому что:
- Сообщения строго типизированы
- Ошибки ловятся компилятором
- Код проще и безопаснее

### 8. Основные понятия Akka
- **ActorSystem** — контейнер для акторов
- **Actor** — логическая единица
- **Message** — immutable-объект
- **ActorRef** — ссылка для отправки сообщений
- **Mailbox** — очередь сообщений

---

## Связь с лабораторной работой

В лабораторной работе №5 мы реализуем все эти концепции на практике:

1. ✅ Создаём `ActorSystem`
2. ✅ Определяем immutable-сообщения (`SayHello`)
3. ✅ Создаём актор с поведением (`HelloActor`)
4. ✅ Отправляем сообщения через `tell()`
5. ✅ Убеждаемся, что порядок гарантирован
6. ✅ Видим, что race condition невозможна

---

## Контрольные вопросы из лекции

### 1. Что такое актор?
Независимая вычислительная сущность, которая владеет своим состоянием, обрабатывает сообщения по одному и общается только сообщениями.

### 2. Почему в акторе нет race condition?
Состояние принадлежит одному актору, сообщения обрабатываются последовательно, прямого доступа к состоянию нет.

### 3. Чем сообщение отличается от метода?
Сообщение — асинхронная отправка без прямого доступа к памяти. Метод — синхронный вызов с прямым доступом.

### 4. Что такое ActorRef?
Ссылка на актор для отправки ему сообщений. Единственный способ взаимодействия с актором.

### 5. Почему сообщения immutable?
Чтобы гарантировать, что сообщение не изменится во время асинхронной доставки и не возникнет проблем с concurrent доступом.

---

**Изучите файлы проекта и запустите эксперименты!**
