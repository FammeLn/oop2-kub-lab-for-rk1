# 🧪 Лабораторная работа №5
## Первый актор на Akka (Java)

### 📚 Цель работы
Изучить основы акторной модели и научиться:
- Создавать ActorSystem
- Писать простые акторы
- Отправлять сообщения через message passing
- Понимать базовый жизненный цикл акторов

---

## 🎯 Ключевые концепции

### Что такое акторная модель?
**Актор** — это независимая вычислительная сущность, которая:
- ✅ Владеет своим **изолированным состоянием**
- ✅ Обрабатывает **сообщения по одному**
- ✅ Общается **только через сообщения**
- ✅ **Не делится памятью** с другими акторами

### Почему в акторной модели нет race condition?
- Состояние принадлежит **одному актору**
- Сообщения обрабатываются **последовательно**
- Никто не может «залезть» в состояние напрямую
- Даже если сообщений много — они в **очереди (mailbox)**

### Акторы vs Объекты

| Объект (ООП) | Актор |
|--------------|-------|
| Методы | Сообщения |
| Общая память | Изолированное состояние |
| Синхронизация | Очередь сообщений |
| Вызов | Асинхронная отправка |

**Важно:** Актор — не объект с методами, а **процесс с почтовым ящиком**.

---

## 🏗️ Структура проекта

```
oop2-lab5/
├── pom.xml                          # Maven конфигурация
├── README.md                        # Этот файл
└── src/
    └── main/
        ├── java/com/akka/lab5/
        │   ├── Command.java         # Интерфейс для типобезопасности
        │   ├── SayHello.java        # Immutable сообщение
        │   ├── HelloActor.java      # Простой актор
        │   └── Main.java            # Точка входа с экспериментами
        └── resources/
            ├── application.conf     # Конфигурация Akka
            └── logback.xml          # Конфигурация логирования
```

---

## 💻 Запуск проекта

### Требования
- Java 17+
- Maven 3.6+

### Сборка и запуск
```bash
cd oop2-lab5

# Сборка проекта
mvn clean compile

# Запуск приложения
mvn exec:java
```

---

## 📖 Разбор кода

### 1. Определение сообщений
```java
interface Command {}

class SayHello implements Command {
    final String name;  // final для immutability
    
    SayHello(String name) {
        this.name = name;
    }
}
```

**Почему `interface`?** — Чтобы ограничить допустимые сообщения (типобезопасность)  
**Почему `final`?** — Сообщения должны быть immutable

### 2. Создание актора
```java
class HelloActor {
    static Behavior<Command> create() {
        return Behaviors.receiveMessage(message -> {
            if (message instanceof SayHello hello) {
                System.out.println("Hello, " + hello.name + "!");
            }
            return Behaviors.same();  // Состояние не меняется
        });
    }
}
```

**Разбор:**
- `Behavior<Command>` — актор принимает только Command
- `receiveMessage` — обработка одного сообщения за раз
- `Behaviors.same()` — актор продолжает с тем же поведением

### 3. Создание ActorSystem и отправка сообщений
```java
public class Main {
    public static void main(String[] args) {
        // Создаём ActorSystem — точка входа
        ActorSystem<Command> system = ActorSystem.create(
            HelloActor.create(),
            "hello-system"
        );
        
        // Асинхронная отправка сообщений
        system.tell(new SayHello("Alice"));
        system.tell(new SayHello("Bob"));
        
        // Завершение работы
        system.terminate();
    }
}
```

**Важно:**
- `ActorSystem` — контейнер для акторов
- `tell()` — асинхронная отправка
- **Порядок сообщений гарантирован**

---

## 🔬 Эксперименты

### Эксперимент 1: Базовая отправка
Программа отправляет несколько сообщений и выводит результат:
```
Hello, Alice!
Hello, Bob!
Hello, Charlie!
```

### Эксперимент 2: 1000 сообщений
Программа отправляет 1000 сообщений и демонстрирует:
- ✅ Порядок сохраняется
- ✅ Нет race condition
- ✅ Нет блокировок в коде

### Дополнительные эксперименты (для самостоятельной работы)
1. Добавьте `Thread.sleep(10)` внутрь актора и убедитесь, что race condition не возникает
2. Создайте новый тип сообщения `SayGoodbye` и обработайте его
3. Добавьте счётчик сообщений в актор (изменяемое состояние)

---

## ❓ Контрольные вопросы

### 1. Что такое актор?
**Ответ:** Актор — это независимая вычислительная сущность, которая владеет своим изолированным состоянием, обрабатывает сообщения последовательно и общается только через message passing.

### 2. Почему в акторе нет race condition?
**Ответ:** Потому что:
- Состояние принадлежит одному актору
- Сообщения обрабатываются строго по одному
- Никто не может напрямую обратиться к состоянию
- Все сообщения попадают в очередь (mailbox)

### 3. Чем сообщение отличается от метода?
**Ответ:**
- **Метод** — синхронный вызов, прямой доступ к памяти
- **Сообщение** — асинхронная отправка, нет прямого доступа
- Сообщение кладётся в очередь и обрабатывается когда актор готов

### 4. Что такое ActorRef?
**Ответ:** `ActorRef` — это ссылка на актор, которая используется для отправки ему сообщений. Это единственный способ взаимодействия с актором — прямого доступа к нему нет.

### 5. Почему сообщения immutable?
**Ответ:** Immutability гарантирует, что:
- Сообщение не изменится во время асинхронной доставки
- Нет проблем с concurrent доступом
- Акторы могут безопасно читать данные из сообщений
- Это фундаментальное требование акторной модели

---

## 🎓 Что происходит при выполнении?

1. **Создаётся ActorSystem** — корневой актор и инфраструктура
2. **Создаётся HelloActor** — дочерний актор с поведением
3. **Сообщения отправляются через `tell()`** — попадают в mailbox
4. **Актор обрабатывает их по одному** — последовательно, без блокировок
5. **System завершается** — все акторы корректно останавливаются

**Ключевое:** Никаких потоков, никаких блокировок в вашем коде!

---

## 🚀 Основные понятия Akka

- **ActorSystem** — контейнер для акторов, корневой актор
- **Actor** — логическая единица с поведением
- **Message** — immutable-объект для коммуникации
- **ActorRef** — ссылка для отправки сообщений
- **Mailbox** — очередь сообщений актора
- **Behavior** — определяет, как актор реагирует на сообщения

---

## ✅ Итоги работы

После выполнения лабораторной работы вы:
- ✅ Понимаете акторную модель
- ✅ Умеете создавать простые акторы
- ✅ Отправляете сообщения через ActorRef
- ✅ Понимаете изоляцию состояния
- ✅ Полностью отказываетесь от `synchronized`
- ✅ Входите в **реактивное мышление**

---

## 📚 Дополнительные ресурсы

- [Официальная документация Akka](https://doc.akka.io/docs/akka/current/)
- [Akka Typed Guide](https://doc.akka.io/docs/akka/current/typed/index.html)
- [Actor Model (Wikipedia)](https://en.wikipedia.org/wiki/Actor_model)

---

## 📝 Заметки

### Akka Typed vs Untyped
Мы используем **Akka Typed** (современный API), потому что:
- Сообщения строго типизированы
- Ошибки ловятся компилятором
- Код проще и безопаснее
- Это стандарт Akka сегодня

### Главное отличие от CompletableFuture

| CompletableFuture | Actor |
|-------------------|-------|
| Цепочка | Процесс |
| Нет состояния | Есть состояние |
| Нет isolation | Полная изоляция |
| Нет supervision | Есть supervision |

**Акторы — следующий уровень абстракции!**

---

**© 2026 OOP2 Лабораторная работа №5**
