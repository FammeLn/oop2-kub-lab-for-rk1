# 🚀 Быстрый старт - Лабораторная работа №5

## Структура проекта

```
oop2-lab5/
├── README.md                    # Полное описание работы
├── LECTURE_NOTES.md             # Конспект лекции
├── pom.xml                      # Maven конфигурация
├── run.sh                       # Скрипт запуска (Linux/Mac)
├── run.bat                      # Скрипт запуска (Windows)
├── .gitignore                   # Исключения для Git
└── src/
    └── main/
        ├── java/com/akka/lab5/
        │   ├── Command.java         # Интерфейс сообщений
        │   ├── SayHello.java        # Класс сообщения
        │   ├── HelloActor.java      # Простой актор
        │   ├── CounterActor.java    # Актор со счётчиком
        │   ├── Main.java            # Основное приложение
        │   └── ExperimentsMain.java # Дополнительные эксперименты
        └── resources/
            ├── application.conf     # Конфигурация Akka
            └── logback.xml          # Конфигурация логов
```

## Как запустить

### Вариант 1: Через скрипт (рекомендуется)

**Linux/Mac:**
```bash
cd oop2-lab5
chmod +x run.sh
./run.sh
```

**Windows:**
```cmd
cd oop2-lab5
run.bat
```

### Вариант 2: Через Maven напрямую

```bash
cd oop2-lab5

# Компиляция
mvn clean compile

# Запуск основного приложения
mvn exec:java -Dexec.mainClass="com.akka.lab5.Main"

# Запуск дополнительных экспериментов
mvn exec:java -Dexec.mainClass="com.akka.lab5.ExperimentsMain"
```

### Вариант 3: Через IDE

1. Откройте папку `oop2-lab5` в IntelliJ IDEA / Eclipse / VS Code
2. Дождитесь загрузки зависимостей Maven
3. Запустите класс `Main.java` или `ExperimentsMain.java`

## Что вы увидите

### Main.java
- Приветствия для Alice, Bob, Charlie
- Отправка 1000 сообщений с гарантией порядка
- Демонстрация отсутствия race condition

### ExperimentsMain.java
- Актор со счётчиком сообщений
- Несколько независимых акторов
- Изолированное состояние

## Требования

- **Java:** 17 или выше
- **Maven:** 3.6 или выше

Проверка версий:
```bash
java -version
mvn -version
```

## Изучение материалов

1. **Начните с LECTURE_NOTES.md** — конспект лекции с основными концепциями
2. **Прочитайте README.md** — полное описание лабораторной работы
3. **Изучите код** в порядке:
   - `Command.java` — интерфейс
   - `SayHello.java` — сообщение
   - `HelloActor.java` — простой актор
   - `Main.java` — основное приложение
   - `CounterActor.java` — актор с состоянием
   - `ExperimentsMain.java` — эксперименты

4. **Запустите приложения** и понаблюдайте за выводом
5. **Ответьте на контрольные вопросы** из README.md

## Дополнительные задания

1. Добавьте новый тип сообщения `SayGoodbye`
2. Создайте актор, который подсчитывает количество гласных букв в именах
3. Добавьте `Thread.sleep(10)` в HelloActor и убедитесь, что race condition не возникает
4. Создайте два актора и переправьте сообщения между ними

## Полезные команды

```bash
# Очистка проекта
mvn clean

# Только компиляция
mvn compile

# Запуск с параметром
mvn exec:java -Dexec.mainClass="com.akka.lab5.Main" -Dexec.args="arg1 arg2"

# Создание JAR файла
mvn package

# Запуск тестов (если есть)
mvn test
```

## Решение проблем

### Ошибка: "Maven не найден"
Установите Maven: https://maven.apache.org/install.html

### Ошибка: "Java version is too old"
Обновите Java до версии 17 или выше

### Ошибка компиляции
Убедитесь, что находитесь в папке `oop2-lab5` и выполните:
```bash
mvn clean compile -U
```

### Зависимости не загружаются
Проверьте подключение к интернету и выполните:
```bash
mvn dependency:resolve
```

## Контакты и поддержка

Если возникли вопросы по лабораторной работе:
1. Изучите README.md — там есть ответы на все контрольные вопросы
2. Перечитайте LECTURE_NOTES.md
3. Проверьте комментарии в коде — они очень подробные

---

**Удачи в изучении акторной модели! 🎓**
