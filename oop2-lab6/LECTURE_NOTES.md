# 📘 ЛЕКЦИЯ 6 - Как из акторов складывается система

## Связь с предыдущей неделей

**Неделя 5:** Познакомились с акторной моделью, написали один актор  
**Неделя 6:** Строим систему из множества взаимодействующих акторов

**Главный вопрос:** Как из множества акторов построить согласованную и масштабируемую систему?

---

## Ключевые концепции

### 1. ActorSystem — корень всей системы

ActorSystem — это:
- Контейнер для акторов
- Среда выполнения
- Владелец ресурсов (потоки, планирование)
- Единица отказа

**Важное правило:** Все акторы живут внутри ActorSystem

### 2. Почему нельзя создавать акторы «где угодно»

В Java мы привыкли: `new MyObject();`

В Akka так нельзя:
- Актор должен быть зарегистрирован
- Его жизненный цикл управляется системой
- Система должна знать, где он живёт

**Акторы создаются только через ActorSystem или другой актор**

### 3. ActorRef — единственный способ общения

ActorRef:
- Это ссылка на актор
- Не даёт доступа к состоянию
- Позволяет только `tell()`

```java
ActorRef<Command> ref;
ref.tell(new Message());
```

**ActorRef:**
- Потокобезопасен
- Можно передавать между акторами
- Можно хранить как поле

**ActorRef = адрес, а не объект**

### 4. Почему нельзя вызывать методы актора

Если бы мы могли: `actor.deposit(100);`

Мы бы:
- Получили shared state
- Сломали изоляцию
- Вернули race condition

**Методы запрещены концептуально**

### 5. Mailbox — сердце актора

Каждый актор имеет mailbox:
- Очередь сообщений
- Сообщения упорядочены
- Обрабатываются по одному

**Важно:**
- Порядок сообщений гарантирован от одного отправителя
- Между разными отправителями порядок не гарантируется

### 6. Последовательность без блокировок

Актор:
- Обрабатывает одно сообщение
- Завершает обработку
- Берёт следующее

Это:
- Не блокировка
- А кооперативная последовательность

**Нет `synchronized`, но есть порядок**

### 7. Dispatcher (интуитивно)

Dispatcher:
- Назначает поток для выполнения актора
- Управляет пулом потоков
- Скрыт от пользователя

**Главное:** Вы не управляете потоками напрямую

### 8. Создание дочерних акторов

Актор может создавать дочерние акторы:
```java
context.spawn(ChildActor.create(), "child");
```

Это формирует:
- Иерархию
- Структуру ответственности
- Основу supervision (следующая тема)

### 9. Передача ActorRef между акторами

Акторы могут:
- Отправлять ActorRef в сообщениях
- Динамически строить граф взаимодействия

Пример:
```java
class Register {
    final ActorRef<Response> replyTo;
}
```

**Это позволяет строить гибкие, динамические системы**

### 10. Request-Response без блокировок

В акторной модели:
- Нет возврата значения
- Используется ответ сообщением

**Шаблон:**
1. Отправка запроса с `replyTo`
2. Обработка
3. Ответ через `tell()`

**Это асинхронный аналог вызова метода**

### 11. Почему акторы хорошо масштабируются

- Нет shared state
- Нет блокировок
- Изоляция по умолчанию
- Легко распределять

**Акторы — архитектурная единица, а не класс**

### 12. Роль Akka в Java-мире

Фреймворк Akka даёт Java-разработчику:
- Безопасную модель параллелизма
- Отказ от ручного управления потоками
- Декларативное поведение
- Реактивное мышление

---

## Задание из лекции

### Реализовать:
- **ClientActor** — отправляет запросы
- **ServiceActor** — обрабатывает запросы и отправляет ответы

### Ключевые требования:
- Использовать Request с полем `replyTo`
- Использовать Response для ответа
- Создавать акторов через `context.spawn()`
- Демонстрировать Request-Response паттерн

---

## Эксперимент для студента

Предлагается:
- Отправить несколько запросов
- Добавить задержку в ServiceActor
- Убедиться, что ClientActor остаётся корректным
- Проверить порядок сообщений

---

## Контрольные вопросы

1. **Что такое ActorSystem?**
2. **Почему нельзя создавать акторы через `new`?**
3. **Что такое ActorRef?**
4. **Как реализуется Request-Response?**
5. **Почему mailbox важен?**

---

## Промежуточные результаты обучения

После Недели 6 студент:
- ✅ Понимает архитектуру ActorSystem
- ✅ Умеет строить систему акторов
- ✅ Использует ActorRef корректно
- ✅ Реализует асинхронное взаимодействие
- ✅ Мыслит message-driven
- ✅ Готов к supervision и состоянию

---

## Связь с лабораторной работой

В лабораторной работе №6 мы реализуем все эти концепции:

1. ✅ Создаём ActorSystem с корневым поведением
2. ✅ Используем `context.spawn()` для создания акторов
3. ✅ Передаём ActorRef в сообщениях (replyTo)
4. ✅ Реализуем Request-Response паттерн
5. ✅ Демонстрируем асинхронное взаимодействие
6. ✅ Проверяем порядок сообщений и работу mailbox

---

**Изучите файлы проекта и запустите эксперименты!**
