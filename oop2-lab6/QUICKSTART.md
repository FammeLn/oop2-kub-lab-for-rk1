# 🚀 Быстрый старт - Лабораторная работа №6

## Структура проекта

```
oop2-lab6/
├── README.md                    # Полное описание работы
├── LECTURE_NOTES.md             # Конспект лекции
├── QUICKSTART.md                # Этот файл
├── pom.xml                      # Maven конфигурация
├── run.sh                       # Скрипт запуска (Linux/Mac)
├── run.bat                      # Скрипт запуска (Windows)
├── .gitignore                   # Исключения для Git
└── src/
    └── main/
        ├── java/com/akka/lab6/
        │   ├── Command.java         # Интерфейс команд
        │   ├── Request.java         # Запрос с replyTo
        │   ├── Response.java        # Ответ от сервиса
        │   ├── ServiceActor.java    # Актор-сервис
        │   ├── ClientActor.java     # Актор-клиент
        │   ├── Main.java            # Основное приложение
        │   └── ExperimentsMain.java # Эксперименты
        └── resources/
            ├── application.conf     # Конфигурация Akka
            └── logback.xml          # Конфигурация логов
```

## Как запустить

### Вариант 1: Через скрипт (рекомендуется)

**Linux/Mac:**
```bash
cd oop2-lab6
chmod +x run.sh
./run.sh
```

**Windows:**
```cmd
cd oop2-lab6
run.bat
```

### Вариант 2: Через Maven напрямую

```bash
cd oop2-lab6

# Компиляция
mvn clean compile

# Запуск основного приложения
mvn exec:java -Dexec.mainClass="com.akka.lab6.Main"

# Запуск экспериментов
mvn exec:java -Dexec.mainClass="com.akka.lab6.ExperimentsMain"
```

### Вариант 3: Через IDE

1. Откройте папку `oop2-lab6` в IntelliJ IDEA / Eclipse / VS Code
2. Дождитесь загрузки зависимостей Maven
3. Запустите класс `Main.java` или `ExperimentsMain.java`

## Что вы увидите

### Main.java
- **Эксперимент 1:** Одиночный запрос и ответ
- **Эксперимент 2:** 5 последовательных запросов
- **Эксперимент 3:** Два клиента, независимая работа

**Демонстрирует:**
- Создание акторов через `context.spawn()`
- Передачу ActorRef в сообщениях
- Request-Response паттерн
- Асинхронное взаимодействие

### ExperimentsMain.java
- Сервис с задержкой обработки (1 секунда на запрос)
- 10 быстрых запросов
- Сохранение порядка при задержках

**Демонстрирует:**
- Работу mailbox
- Отсутствие блокировок
- Асинхронную обработку

## Требования

- **Java:** 17 или выше
- **Maven:** 3.6 или выше

Проверка версий:
```bash
java -version
mvn -version
```

## Изучение материалов

### Порядок изучения:

1. **LECTURE_NOTES.md** — конспект лекции с теорией
2. **README.md** — полное описание лабораторной
3. **Код в порядке:**
   - `Command.java` — интерфейс
   - `Request.java` и `Response.java` — сообщения
   - `ServiceActor.java` — сервис
   - `ClientActor.java` — клиент
   - `Main.java` — основное приложение
   - `ExperimentsMain.java` — эксперименты

4. **Запустите приложения** и изучите вывод
5. **Ответьте на контрольные вопросы** из README.md

## Ключевые моменты

### ActorRef передаётся в сообщении
```java
// В Request есть поле replyTo
class Request {
    final ActorRef<Response> replyTo;
}

// ServiceActor использует его для ответа
req.replyTo.tell(new Response("result"));
```

### Создание акторов через spawn
```java
// Создание дочерних акторов
var service = context.spawn(ServiceActor.create(), "service");
var client = context.spawn(ClientActor.create(), "client");

// Отправка запроса с ActorRef клиента
service.tell(new Request("data", client));
```

### Асинхронное взаимодействие
- Нет возврата значений
- Нет ожиданий
- Нет блокировок
- Всё через сообщения

## Полезные команды

```bash
# Очистка проекта
mvn clean

# Только компиляция
mvn compile

# Запуск с параметром
mvn exec:java -Dexec.mainClass="com.akka.lab6.Main"

# Создание JAR файла
mvn package
```

## Решение проблем

### Ошибка: "Maven не найден"
Установите Maven: https://maven.apache.org/install.html

### Ошибка: "Java version is too old"
Обновите Java до версии 17 или выше

### Ошибка компиляции
```bash
mvn clean compile -U
```

### Зависимости не загружаются
```bash
mvn dependency:resolve
```

## Дополнительные задания

После изучения основного материала:

1. Добавьте актор `LoggerActor` для логирования всех запросов
2. Создайте счётчик запросов в ServiceActor
3. Реализуйте актор-координатор для балансировки нагрузки
4. Добавьте обработку ошибок в ServiceActor
5. Создайте цепочку из 3-4 акторов

## Контрольные вопросы

Убедитесь, что можете ответить на:
1. Что такое ActorSystem?
2. Почему нельзя создавать акторы через `new`?
3. Что такое ActorRef?
4. Как реализуется Request-Response?
5. Почему mailbox важен?

Ответы в [README.md](README.md)

---

**Удачи в изучении взаимодействия акторов! 🎓**
